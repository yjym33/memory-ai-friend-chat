# 🤖 AI Chatbot - 지능형 대화형 챗봇 플랫폼

> **감성과 지능을 갖춘 개인 맞춤형 AI 비서**  
> NestJS + Next.js 기반 풀스택 챗봇 애플리케이션

---

## 📋 목차

- [프로젝트 개요](#-프로젝트-개요)
- [주요 기능](#-주요-기능)
- [기술 스택](#-기술-스택)
- [시스템 아키텍처](#-시스템-아키텍처)
- [프로젝트 구조](#-프로젝트-구조)
- [주요 모듈 상세](#-주요-모듈-상세)
- [설치 및 실행](#-설치-및-실행)
- [API 문서](#-api-문서)
- [개발 과정](#-개발-과정)
- [향후 계획](#-향후-계획)

---

## 🎯 프로젝트 개요

### 프로젝트 소개

AI Chatbot은 사용자와의 자연스러운 대화를 통해 **감정 분석**, **목표 관리**, **문서 기반 질의응답** 등 다양한 기능을 제공하는 지능형 챗봇 플랫폼입니다. 개인과 기업 모두를 위한 맞춤형 AI 솔루션을 제공합니다.

### 핵심 가치

- 🧠 **지능형 대화**: OpenAI GPT-4 기반의 자연스러운 대화
- 💝 **감성 인식**: 사용자의 감정을 이해하고 공감하는 AI
- 🎯 **목표 관리**: AI 에이전트를 통한 개인 목표 달성 지원
- 📚 **RAG 기반 QA**: 기업 문서를 학습하여 정확한 답변 제공
- 🎨 **개인화**: 사용자별 AI 성격, 말투, 이모지 사용 커스터마이징

### 프로젝트 정보

- **개발 기간**: 2024년 11월 ~ 현재
- **개발 인원**: 1명 (풀스택)
- **버전**: v0.1.0
- **라이선스**: UNLICENSED (Private)

---

## ✨ 주요 기능

### 1. 🗣️ 실시간 AI 대화

- **스트리밍 방식 응답**: Server-Sent Events (SSE)를 활용한 실시간 토큰 스트리밍
- **컨텍스트 유지**: 최근 6개 메시지(3턴)를 기억하여 문맥 파악
- **다양한 대화 모드**:
  - 📱 **개인 모드**: 친구처럼 편안한 대화
  - 💼 **기업 모드**: 문서 기반 전문적인 QA

### 2. 🎭 AI 에이전트 시스템

- **감정 인식 및 분석**:
  - 대화 중 사용자의 감정 상태 실시간 감지
  - 긍정/부정/중립 감정 추적
  - 시계열 감정 변화 시각화
- **목표 관리 (Life Coach Agent)**:

  - 사용자 목표 설정 및 추적
  - 마일스톤 관리 및 진행률 계산
  - AI 기반 목표 달성 조언
  - 목표 달성 시 자동 축하 메시지

- **에이전트 워크플로우**:
  ```
  사용자 메시지
      ↓
  감정 분석 (Sentiment Analysis)
      ↓
  목표 추출 (Goal Extraction)
      ↓
  컨텍스트 로딩
      ↓
  후속 조치 필요 여부 판단
      ↓
  맞춤형 지원 메시지 생성
  ```

### 3. 🎨 개인화 설정

- **AI 성격 커스터마이징**:
  - 4가지 성격 유형: 친근함, 전문적, 유머러스, 차분함
  - 3가지 말투: 반말, 존댓말, 친근한 존댓말
- **이모지 사용량 조절**:
  - 5단계 조절 (없음 ~ 매우 많음)
- **닉네임 설정**:

  - AI가 사용자를 부르는 호칭 지정

- **테마 커스터마이징**:
  - 채팅 배경색, 버블 색상, 텍스트 색상 등

### 4. 📚 RAG 기반 문서 QA (기업 모드)

- **지원 문서 형식**:
  - PDF, DOCX, XLSX, TXT
- **문서 처리 파이프라인**:

  ```
  파일 업로드
      ↓
  텍스트 추출 (FileExtractionService)
      ↓
  청크 분할 (Chunking)
      ↓
  벡터 임베딩 생성 (Vector Embeddings)
      ↓
  벡터 DB 저장
      ↓
  의미 기반 검색 (Semantic Search)
      ↓
  관련 문서와 함께 LLM에게 전달
      ↓
  출처 포함 답변 생성
  ```

- **고급 검색 기능**:
  - 의미 기반 검색
  - 문서 메타데이터 필터링
  - 관련도(Relevance) 스코어링
  - 출처 표시 (Source Attribution)

### 5. 👥 사용자 인증 및 권한 관리

- **인증 방식**:
  - JWT 기반 인증
  - Google OAuth 2.0 소셜 로그인
  - Kakao OAuth 소셜 로그인
- **사용자 유형**:

  - 👤 **개인 사용자**: 기본 채팅 및 개인화 기능
  - 🏢 **기업 사용자**: 문서 관리 및 팀 기능
  - 👑 **관리자**: 전체 시스템 관리

- **조직 관리**:
  - 다중 조직 지원
  - 조직별 문서 및 사용자 격리
  - 조직 관리자 권한

### 6. 📊 대화 분석 (Analytics)

- **통계 정보**:
  - 총 대화 수
  - 평균 대화 길이
  - 주제별 대화 분류
  - 감정 분포
- **시각화**:
  - 시계열 대화 트렌드
  - 감정 변화 그래프
  - 목표 달성률 차트

### 7. 🔐 관리자 페이지

- **사용자 관리**:
  - 사용자 목록 조회
  - 사용자 유형 변경
  - 사용자 통계
- **문서 관리**:
  - 전체 문서 목록
  - 임베딩 상태 확인
  - 문서 삭제 및 재처리
- **조직 관리**:
  - 조직 생성/수정/삭제
  - 조직 구성원 관리

---

## 🛠️ 기술 스택

### 백엔드 (Backend)

| 카테고리         | 기술                     | 버전   | 용도            |
| ---------------- | ------------------------ | ------ | --------------- |
| **프레임워크**   | NestJS                   | 10.x   | 서버 프레임워크 |
| **언어**         | TypeScript               | 5.x    | 타입 안정성     |
| **데이터베이스** | PostgreSQL               | 12+    | 메인 DB         |
| **ORM**          | TypeORM                  | 0.3.x  | DB 추상화       |
| **인증**         | Passport.js, JWT         | -      | 인증/인가       |
| **AI/LLM**       | OpenAI GPT-4             | 4.x    | 대화 생성       |
| **감정 분석**    | Natural, Sentiment       | -      | NLP 처리        |
| **파일 처리**    | pdf-parse, mammoth, xlsx | -      | 문서 추출       |
| **로깅**         | Winston                  | 3.x    | 구조화된 로깅   |
| **검증**         | class-validator          | 0.14.x | DTO 검증        |

### 프론트엔드 (Frontend)

| 카테고리            | 기술            | 버전    | 용도             |
| ------------------- | --------------- | ------- | ---------------- |
| **프레임워크**      | Next.js 15      | 15.1.x  | React 프레임워크 |
| **언어**            | TypeScript      | 5.x     | 타입 안정성      |
| **UI 라이브러리**   | React 19        | 19.0.x  | UI 컴포넌트      |
| **상태 관리**       | Zustand         | 5.x     | 전역 상태        |
| **스타일링**        | Tailwind CSS    | 3.4.x   | 유틸리티 CSS     |
| **애니메이션**      | Framer Motion   | 12.x    | UI 애니메이션    |
| **HTTP 클라이언트** | Axios           | 1.7.x   | API 통신         |
| **데이터 캐싱**     | React Query     | 5.x     | 서버 상태 관리   |
| **마크다운**        | react-markdown  | 10.x    | 메시지 렌더링    |
| **아이콘**          | Lucide React    | 0.511.x | 아이콘 세트      |
| **토스트**          | react-hot-toast | 2.x     | 알림 메시지      |

### 개발 도구

- **패키지 매니저**: npm
- **빌드 도구**: Next.js (Turbopack), NestJS CLI
- **테스팅**: Jest, React Testing Library
- **린터**: ESLint, Prettier
- **버전 관리**: Git

---

## 🏗️ 시스템 아키텍처

### 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Chat UI    │  │  Admin UI    │  │  Settings    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                  │                  │              │
│         └──────────────────┴──────────────────┘              │
│                            ↓                                 │
│                     API Client (Axios)                       │
└────────────────────────────┬────────────────────────────────┘
                             │ REST API / SSE
                             ↓
┌─────────────────────────────────────────────────────────────┐
│                    Backend (NestJS)                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Gateway & Auth Guard                 │  │
│  └──────────────────────────────────────────────────────┘  │
│         │              │              │              │       │
│         ↓              ↓              ↓              ↓       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │   Chat   │  │  Agent   │  │ Document │  │  Admin   │  │
│  │  Module  │  │  Module  │  │  Module  │  │  Module  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│         │              │              │              │       │
│         └──────────────┴──────────────┴──────────────┘       │
│                            ↓                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Common Services & Utilities                 │  │
│  │  • LlmService  • FileExtractionService               │  │
│  │  • VectorService  • PerformanceMetrics               │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ↓
              ┌──────────────────────────────┐
              │     PostgreSQL Database      │
              │  • Users  • Conversations    │
              │  • Documents  • Emotions     │
              │  • Goals  • Analytics        │
              └──────────────────────────────┘
                             │
                             ↓
              ┌──────────────────────────────┐
              │    External Services         │
              │  • OpenAI GPT-4 API          │
              │  • Google OAuth              │
              │  • Kakao OAuth               │
              └──────────────────────────────┘
```

### 데이터 흐름 (Data Flow)

#### 1. 일반 대화 흐름

```
User Input → Frontend (useChat Hook)
    ↓
Backend Chat Controller
    ↓
Chat Service → LLM Service
    ↓
OpenAI API (GPT-4)
    ↓
SSE Streaming Response
    ↓
Frontend Real-time Update
    ↓
Database Save (Conversation)
```

#### 2. 기업 모드 RAG 흐름

```
User Query → Frontend
    ↓
Backend Chat Controller
    ↓
Document Service → Vector Search
    ↓
Top-K Relevant Chunks Retrieved
    ↓
LLM Service (Prompt with Context)
    ↓
OpenAI API (GPT-4 with RAG)
    ↓
Response with Sources
    ↓
Frontend Display with Citations
```

#### 3. 에이전트 워크플로우

```
User Message → Agent Service
    ↓
Emotion Analysis (Sentiment Library)
    ↓
Goal Extraction (NLP + GPT)
    ↓
Context Loading (Recent Emotions + Goals)
    ↓
Decision Making (Follow-up needed?)
    ↓
Support Message Generation
    ↓
Database Save (Emotions, Goals)
```

---

## 📁 프로젝트 구조

### 백엔드 구조

```
chatbot-backend/
├── src/
│   ├── main.ts                          # 애플리케이션 엔트리 포인트
│   ├── app.module.ts                    # 루트 모듈
│   │
│   ├── auth/                            # 🔐 인증 모듈
│   │   ├── auth.controller.ts           # 로그인/회원가입 API
│   │   ├── auth.service.ts              # JWT 토큰 발급/검증
│   │   ├── guards/
│   │   │   └── jwt-auth.guard.ts        # JWT 인증 가드
│   │   ├── strategies/
│   │   │   ├── google.strategy.ts       # Google OAuth
│   │   │   └── kakao.strategy.ts        # Kakao OAuth
│   │   └── entity/
│   │       ├── user.entity.ts           # 사용자 엔티티
│   │       └── organization.entity.ts   # 조직 엔티티
│   │
│   ├── chat/                            # 💬 채팅 모듈
│   │   ├── chat.controller.ts           # 채팅 API (SSE 포함)
│   │   ├── chat.service.ts              # 채팅 비즈니스 로직
│   │   └── entity/
│   │       └── conversation.entity.ts   # 대화 엔티티
│   │
│   ├── agent/                           # 🤖 AI 에이전트 모듈
│   │   ├── agent.service.ts             # 감정 분석, 목표 관리
│   │   ├── entities/
│   │   │   ├── emotion.entity.ts        # 감정 기록
│   │   │   ├── goal.entity.ts           # 목표
│   │   │   └── milestone.entity.ts      # 마일스톤
│   │   └── workflows/
│   │       └── life-coach-workflow.ts   # Life Coach 워크플로우
│   │
│   ├── document/                        # 📚 문서 관리 모듈
│   │   ├── document.service.ts          # 문서 CRUD
│   │   ├── vector.service.ts            # 벡터 임베딩/검색
│   │   └── entity/
│   │       ├── document.entity.ts       # 문서 메타데이터
│   │       ├── document-chunk.entity.ts # 문서 청크
│   │       └── document-version.entity.ts # 문서 버전
│   │
│   ├── ai-settings/                     # ⚙️ AI 설정 모듈
│   │   ├── ai-settings.service.ts       # AI 성격, 말투 설정
│   │   └── entity/
│   │       └── ai-settings.entity.ts    # AI 설정 엔티티
│   │
│   ├── admin/                           # 👑 관리자 모듈
│   │   ├── admin.controller.ts          # 관리자 API
│   │   └── admin.service.ts             # 관리자 비즈니스 로직
│   │
│   ├── common/                          # 🔧 공통 모듈
│   │   ├── constants/
│   │   │   └── llm.constants.ts         # LLM 설정 상수
│   │   ├── services/
│   │   │   ├── llm.service.ts           # OpenAI API 래퍼
│   │   │   ├── file-extraction.service.ts # 파일 추출
│   │   │   └── performance-metrics.service.ts # 성능 모니터링
│   │   ├── utils/
│   │   │   ├── conversation.utils.ts    # 대화 유틸리티
│   │   │   └── error-handler.utils.ts   # 에러 처리
│   │   ├── filters/
│   │   │   └── global-exception.filter.ts # 글로벌 예외 필터
│   │   └── middleware/
│   │       ├── logger.middleware.ts      # 로깅 미들웨어
│   │       └── performance-tracking.middleware.ts # 성능 추적
│   │
│   ├── config/                          # 🔧 설정
│   │   ├── database.config.ts           # DB 설정
│   │   ├── security.config.ts           # 보안 설정
│   │   ├── logger.config.ts             # 로거 설정
│   │   └── env.validation.ts            # 환경변수 검증
│   │
│   └── migrations/                      # 📦 데이터베이스 마이그레이션
│       ├── 1700000000000-InitialSchema.ts
│       ├── 1700000000001-AddOrganizationAndUserTypes.ts
│       └── 1700000000002-AddDocumentTables.ts
│
├── test/                                # 테스트
├── uploads/                             # 업로드된 파일
├── logs/                                # 로그 파일
└── dist/                                # 빌드 결과물
```

### 프론트엔드 구조

```
chatbot-frontend/
├── src/
│   ├── app/                             # Next.js App Router
│   │   ├── layout.tsx                   # 루트 레이아웃
│   │   ├── page.tsx                     # 홈 페이지
│   │   ├── providers.tsx                # 전역 프로바이더
│   │   ├── chat/[userId]/
│   │   │   └── page.tsx                 # 채팅 페이지
│   │   ├── login/
│   │   │   └── page.tsx                 # 로그인
│   │   ├── register/
│   │   │   └── page.tsx                 # 회원가입
│   │   ├── admin/
│   │   │   └── page.tsx                 # 관리자 페이지
│   │   ├── documents/
│   │   │   └── page.tsx                 # 문서 관리
│   │   └── our-stories/
│   │       └── page.tsx                 # 대화 기록
│   │
│   ├── components/                      # React 컴포넌트
│   │   ├── Chatbot.tsx                  # 메인 채팅 컴포넌트
│   │   ├── ChatWindow.tsx               # 메시지 표시
│   │   ├── ChatInput.tsx                # 메시지 입력
│   │   ├── MessageBubble.tsx            # 메시지 버블
│   │   ├── ChatListSidebar.tsx          # 대화 목록
│   │   ├── ChatModeSwitch.tsx           # 모드 전환
│   │   ├── AiSettingsModal.tsx          # AI 설정 모달
│   │   ├── AgentStatusModal.tsx         # 에이전트 상태
│   │   ├── goal-management/
│   │   │   └── GoalManager.tsx          # 목표 관리 UI
│   │   ├── agent-status/
│   │   │   ├── ActiveGoals.tsx          # 활성 목표 표시
│   │   │   ├── EmotionSummary.tsx       # 감정 요약
│   │   │   └── RecentEmotions.tsx       # 최근 감정
│   │   ├── theme/
│   │   │   └── ThemeSelector.tsx        # 테마 선택기
│   │   └── monitoring/
│   │       └── MonitoringDashboard.tsx  # 모니터링 대시보드
│   │
│   ├── hooks/                           # Custom Hooks
│   │   ├── useChat.ts                   # 채팅 관리
│   │   ├── useAuth.ts                   # 인증 관리
│   │   ├── useAiSettings.ts             # AI 설정
│   │   ├── useAgentStatus.ts            # 에이전트 상태
│   │   ├── useTheme.ts                  # 테마 관리
│   │   ├── useErrorHandler.ts           # 에러 처리
│   │   └── useWebSocket.ts              # WebSocket 연결
│   │
│   ├── services/                        # API 서비스
│   │   ├── apiClient.ts                 # Axios 인스턴스
│   │   ├── authService.ts               # 인증 API
│   │   ├── chatService.ts               # 채팅 API
│   │   ├── aiSettingsService.ts         # AI 설정 API
│   │   └── agentService.ts              # 에이전트 API
│   │
│   ├── store/                           # Zustand 스토어
│   │   ├── authStore.ts                 # 인증 상태
│   │   └── chatStore.ts                 # 채팅 상태
│   │
│   ├── utils/                           # 유틸리티 함수
│   │   ├── conversationHelpers.ts       # 대화 헬퍼
│   │   ├── emojiMaps.ts                 # 이모지 매핑
│   │   └── templateUtils.ts             # 템플릿 유틸
│   │
│   ├── constants/                       # 상수
│   │   └── messages.ts                  # 에러/성공 메시지
│   │
│   ├── types/                           # TypeScript 타입
│   │   ├── index.ts                     # 공통 타입
│   │   ├── error.ts                     # 에러 타입
│   │   └── theme.ts                     # 테마 타입
│   │
│   └── lib/                             # 라이브러리
│       ├── queryClient.ts               # React Query 설정
│       ├── toast.ts                     # Toast 설정
│       └── logger.ts                    # 클라이언트 로거
│
├── public/                              # 정적 파일
└── .next/                               # Next.js 빌드
```

---

## 🔍 주요 모듈 상세

### 1. Chat Module (채팅 모듈)

#### 주요 기능

- 실시간 스트리밍 대화
- 대화 내역 저장 및 조회
- 대화 제목 자동 생성
- 대화 고정/보관 기능

#### 핵심 API

```typescript
// 스트리밍 방식 채팅
POST /chat/completion/:conversationId/stream
Headers: {
  "Content-Type": "text/event-stream"
}
Body: {
  "message": "안녕하세요"
}

// SSE 응답 형식
data: {"type":"token","content":"안"}
data: {"type":"token","content":"녕"}
data: {"type":"sources","content":[...]}
data: {"type":"done"}
```

#### 주요 서비스

- **ChatService**: 대화 CRUD, 모드 전환
- **LlmService**: OpenAI API 통신, 스트리밍 처리
- **FileExtractionService**: 파일에서 텍스트 추출

### 2. Agent Module (AI 에이전트 모듈)

#### Life Coach Agent 워크플로우

```typescript
interface AgentState {
  userId: string;
  currentMessage: string;
  detectedEmotions: Emotion[]; // 감지된 감정
  extractedGoals: Goal[]; // 추출된 목표
  recentEmotions: Emotion[]; // 최근 감정
  activeGoals: Goal[]; // 활성 목표
  actions: string[]; // 수행할 액션
  shouldFollowUp: boolean; // 후속 조치 필요 여부
}
```

#### 감정 분석

- **라이브러리**: `sentiment`, `natural`
- **감지 감정**: happy, sad, angry, anxious, excited 등
- **감정 강도**: -1.0 (매우 부정) ~ 1.0 (매우 긍정)

#### 목표 관리

```typescript
interface Goal {
  id: number;
  userId: string;
  title: string; // 목표 제목
  description: string; // 상세 설명
  category: string; // 카테고리 (건강, 학습 등)
  targetDate: Date; // 목표 달성 기한
  progress: number; // 진행률 (0-100)
  status: "active" | "completed" | "abandoned";
  milestones: Milestone[]; // 마일스톤 목록
}
```

### 3. Document Module (문서 관리 모듈)

#### RAG 구현

```typescript
// 1. 문서 업로드 및 처리
POST /document/upload
- 파일 업로드
- 텍스트 추출
- 청크 분할 (500자 단위, 100자 오버랩)
- 임베딩 생성 (OpenAI text-embedding-ada-002)
- 벡터 DB 저장

// 2. 의미 기반 검색
POST /document/search
Body: {
  "query": "검색어",
  "limit": 5,
  "minRelevance": 0.7
}
Response: {
  "results": [
    {
      "documentId": "...",
      "title": "...",
      "content": "...",
      "relevance": 0.95,
      "snippet": "..."
    }
  ]
}
```

#### 지원 파일 형식

| 형식 | 추출 라이브러리 | 특징                   |
| ---- | --------------- | ---------------------- |
| PDF  | pdf-parse       | 텍스트, 표, 메타데이터 |
| DOCX | mammoth         | 서식 유지, 표 추출     |
| XLSX | xlsx            | 시트별 데이터 추출     |
| TXT  | fs              | 직접 읽기              |

### 4. AI Settings Module (AI 설정 모듈)

#### 설정 가능 항목

```typescript
interface AiSettings {
  userId: string;

  // 성격 설정
  personalityType: "친근함" | "전문적" | "유머러스" | "차분함";

  // 말투 설정
  speechStyle: "반말" | "존댓말" | "친근한 존댓말";

  // 이모지 사용량 (0-5)
  emojiUsage: number;

  // 닉네임
  nickname: string;

  // 채팅 모드
  chatMode: "personal" | "business";

  // 메모리 설정
  memoryEnabled: boolean;
  memoryDepth: number; // 기억할 대화 턴 수
}
```

#### 시스템 프롬프트 생성

```typescript
function buildPersonalSystemPrompt(settings: AiSettings): string {
  return `You are a friendly AI companion. Follow these guidelines:
1. Personality: ${settings.personalityType}
2. Language: Respond in Korean using ${settings.speechStyle}
3. Emoji: Use ${getEmojiLevel(settings.emojiUsage)} emojis
4. Call the user: "${settings.nickname}"

IMPORTANT RULES:
- Give ONE clear, concise answer
- Do NOT repeat the same words or phrases
- Keep responses natural and conversational`;
}
```

### 5. Admin Module (관리자 모듈)

#### 관리 기능

- 사용자 관리: 목록, 상세, 유형 변경, 삭제
- 문서 관리: 목록, 임베딩 상태, 재처리
- 조직 관리: 생성, 수정, 구성원 관리
- 통계: 사용자 수, 문서 수, 대화 수 등

#### 권한 체계

```typescript
enum Role {
  SUPER_ADMIN = 'super_admin',  // 시스템 관리자
  ORG_ADMIN = 'org_admin',       // 조직 관리자
  USER = 'user'                  // 일반 사용자
}

// 가드 적용
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(Role.SUPER_ADMIN)
async getUserList() { ... }
```

---

## 🚀 설치 및 실행

### 1. 사전 요구사항

```bash
# Node.js 18+
node --version

# PostgreSQL 12+
psql --version

# npm
npm --version
```

### 2. 데이터베이스 설정

```sql
-- PostgreSQL 데이터베이스 생성
CREATE DATABASE chatbot;

-- 사용자 생성 (옵션)
CREATE USER chatbot_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE chatbot TO chatbot_user;
```

### 3. 환경변수 설정

#### 백엔드 (.env)

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password
DB_NAME=chatbot

# JWT
JWT_SECRET=your-very-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# OpenAI
OPENAI_API_KEY=sk-...your-openai-api-key

# OAuth (선택)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:8080/auth/google/callback

KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CALLBACK_URL=http://localhost:8080/auth/kakao/callback

# Server
PORT=8080
NODE_ENV=development
```

#### 프론트엔드 (.env.local)

```env
NEXT_PUBLIC_API_URL=http://localhost:8080
```

### 4. 설치 및 실행

#### 백엔드

```bash
# 의존성 설치
cd chatbot-backend
npm install

# 데이터베이스 마이그레이션 실행
npm run migration:run

# 개발 서버 실행
npm run start:dev

# 프로덕션 빌드
npm run build
npm run start:prod
```

#### 프론트엔드

```bash
# 의존성 설치
cd chatbot-frontend
npm install

# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build
npm start
```

### 5. 접속

- **프론트엔드**: http://localhost:3000
- **백엔드 API**: http://localhost:8080
- **API 문서**: http://localhost:8080/api (Swagger - 추후 추가 예정)

---

## 📖 API 문서

### 인증 API

#### 회원가입

```http
POST /auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123",
  "name": "홍길동",
  "gender": "male",
  "birthYear": 1990,
  "userType": "individual"
}

Response:
{
  "userId": "uuid",
  "token": "jwt-token",
  "userType": "individual",
  "role": "user"
}
```

#### 로그인

```http
POST /auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}

Response:
{
  "userId": "uuid",
  "token": "jwt-token",
  "userType": "individual",
  "role": "user"
}
```

#### Google 소셜 로그인

```http
GET /auth/google
→ Redirects to Google OAuth

GET /auth/google/callback?code=...
→ Processes OAuth and redirects to frontend with token
```

### 채팅 API

#### 대화 목록 조회

```http
GET /chat/conversations
Authorization: Bearer {token}

Response:
[
  {
    "id": 1,
    "title": "AI와의 첫 대화",
    "messages": [...],
    "pinned": false,
    "isArchived": false,
    "createdAt": "2024-11-17T...",
    "updatedAt": "2024-11-17T..."
  }
]
```

#### 스트리밍 채팅

```http
POST /chat/completion/1/stream
Authorization: Bearer {token}
Content-Type: application/json

{
  "message": "오늘 기분이 좋아요!"
}

Response (SSE):
data: {"type":"token","content":"기"}
data: {"type":"token","content":"분"}
data: {"type":"token","content":"이"}
...
data: {"type":"done"}
```

### 에이전트 API

#### 감정 목록 조회

```http
GET /agent/emotions
Authorization: Bearer {token}

Response:
[
  {
    "id": 1,
    "userId": "...",
    "emotion": "happy",
    "intensity": 0.8,
    "context": "오늘 기분이 좋아요!",
    "detectedAt": "2024-11-17T..."
  }
]
```

#### 목표 생성

```http
POST /agent/goals
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "건강한 생활 습관 만들기",
  "description": "매일 운동하고 건강식 먹기",
  "category": "건강",
  "targetDate": "2024-12-31"
}

Response:
{
  "id": 1,
  "userId": "...",
  "title": "건강한 생활 습관 만들기",
  "progress": 0,
  "status": "active"
}
```

### 문서 API

#### 문서 업로드

```http
POST /document/upload
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
  "file": <binary>,
  "title": "회사 규정",
  "description": "회사 내부 규정 문서",
  "category": "규정"
}

Response:
{
  "id": "doc-uuid",
  "title": "회사 규정",
  "status": "processing",
  "chunkCount": 50
}
```

#### 문서 검색 (RAG)

```http
POST /document/search
Authorization: Bearer {token}
Content-Type: application/json

{
  "query": "휴가 규정은 어떻게 되나요?",
  "limit": 5
}

Response:
{
  "results": [
    {
      "documentId": "...",
      "title": "회사 규정",
      "content": "휴가 규정: 연차는...",
      "relevance": 0.92,
      "snippet": "...휴가 규정..."
    }
  ]
}
```

---

## 🔄 개발 과정

### Phase 1: 기본 채팅 기능 (Week 1-2)

- ✅ NestJS 백엔드 구조 설계
- ✅ Next.js 프론트엔드 초기 설정
- ✅ 기본 인증 시스템 (JWT)
- ✅ PostgreSQL 데이터베이스 설계
- ✅ 간단한 채팅 UI

### Phase 2: AI 통합 (Week 3-4)

- ✅ OpenAI GPT-4 API 연동
- ✅ 실시간 스트리밍 응답 (SSE)
- ✅ 대화 컨텍스트 관리
- ✅ AI 설정 기능 (성격, 말투)

### Phase 3: 에이전트 시스템 (Week 5-6)

- ✅ 감정 분석 엔진
- ✅ 목표 관리 시스템
- ✅ Life Coach Agent 워크플로우
- ✅ 에이전트 상태 대시보드

### Phase 4: RAG & 문서 관리 (Week 7-8)

- ✅ 파일 업로드 및 텍스트 추출
- ✅ 문서 청킹 및 임베딩
- ✅ 벡터 검색 구현
- ✅ RAG 기반 QA

### Phase 5: OAuth & 고급 기능 (Week 9-10)

- ✅ Google OAuth 2.0
- ✅ Kakao OAuth
- ✅ 조직 관리 시스템
- ✅ 관리자 페이지

### Phase 6: 리팩토링 & 최적화 (Week 11-12)

- ✅ 코드 리팩토링 (DRY 원칙 적용)
- ✅ 서비스 분리 (LlmService, FileExtractionService)
- ✅ 상수 추출 및 에러 처리 통합
- ✅ 성능 모니터링 추가

### 주요 개선 사항

1. **코드 중복 제거**: 530줄 → 340줄 (36% 감소)
2. **관심사 분리**: 파일 추출, LLM 통신 로직 분리
3. **타입 안정성**: TypeScript 엄격 모드 적용
4. **에러 처리**: 통합된 에러 핸들링
5. **성능 최적화**: React 불변성 원칙 준수

---

## 🐛 알려진 이슈 및 해결

### 1. 스트리밍 UI 깜빡임 현상

**문제**: 스트리밍 중 UI가 깜빡이거나 이전 내용이 사라짐
**원인**: React 상태 업데이트 시 불변성 미준수
**해결**: 헬퍼 함수를 통한 올바른 불변성 업데이트

```typescript
// 이전 (잘못된 방법)
messages[lastIndex].content += token;

// 이후 (올바른 방법)
messages[lastIndex] = {
  ...messages[lastIndex],
  content: messages[lastIndex].content + token,
};
```

### 2. LLM 응답 반복 현상

**문제**: AI가 같은 단어를 반복하거나 특수문자 출력
**원인**:

- UTF-8 디코딩 문제
- OpenAI 파라미터 미최적화
- 시스템 프롬프트 모호함

**해결**:

- 버퍼링을 통한 UTF-8 디코딩
- `frequency_penalty: 0.5`, `presence_penalty: 0.3` 추가
- 영어로 명확한 시스템 프롬프트 작성

### 3. OAuth 설정 누락 시 서버 시작 실패

**문제**: OAuth 환경변수가 없으면 서버가 시작되지 않음
**해결**: Dummy 값 사용 + `isConfigured` 플래그로 런타임 비활성화

---

## 🔮 향후 계획

### 단기 목표 (1-2개월)

- [ ] **테스트 커버리지 80% 달성**
  - 단위 테스트 (Jest)
  - E2E 테스트 (Playwright)
- [ ] **WebSocket 실시간 통신**
  - 양방향 실시간 채팅
  - 타이핑 인디케이터
- [ ] **다국어 지원 (i18n)**

  - 한국어, 영어
  - 언어별 AI 프롬프트

- [ ] **음성 인식/합성**
  - Web Speech API
  - TTS (Text-to-Speech)

### 중기 목표 (3-6개월)

- [ ] **고급 RAG 기능**
  - 하이브리드 검색 (키워드 + 벡터)
  - Re-ranking
  - Query Expansion
- [ ] **멀티모달 지원**
  - 이미지 업로드 및 분석
  - GPT-4 Vision 활용
- [ ] **협업 기능**

  - 팀 채팅방
  - 공유 문서
  - 멘션 및 알림

- [ ] **분석 대시보드**
  - 사용량 통계
  - 감정 트렌드 분석
  - 목표 달성률 시각화

### 장기 목표 (6개월+)

- [ ] **에이전트 확장**
  - 다양한 에이전트 (학습 코치, 건강 관리 등)
  - 에이전트 마켓플레이스
- [ ] **플러그인 시스템**
  - 써드파티 통합 (Notion, Slack 등)
  - 커스텀 플러그인 개발 SDK
- [ ] **모바일 앱**
  - React Native
  - 네이티브 푸시 알림
- [ ] **오픈소스 공개**
  - MIT 라이선스
  - 커뮤니티 기여

---

## 📊 성능 지표

### 현재 성능

- **평균 응답 시간**: ~2초 (스트리밍 시작까지)
- **동시 접속자 처리**: ~100명 (테스트 환경)
- **문서 검색 속도**: ~500ms (벡터 DB 쿼리)
- **메모리 사용량**: ~300MB (백엔드 프로세스)

### 최적화 목표

- **응답 시간**: 1초 이내
- **동시 접속자**: 1000명+
- **문서 검색**: 200ms 이내
- **메모리 사용량**: 200MB 이하

---

## 🤝 기여 방법

현재는 개인 프로젝트로 진행 중이지만, 향후 오픈소스 공개를 계획하고 있습니다.

### 기여 가능 영역

1. **버그 수정**: 이슈 등록 및 PR
2. **새 기능**: 기능 제안 및 구현
3. **문서화**: README, API 문서 개선
4. **번역**: 다국어 지원
5. **테스트**: 테스트 코드 작성

---

## 📝 라이선스

현재는 비공개(UNLICENSED) 프로젝트입니다.
향후 MIT 라이선스로 오픈소스 전환 예정입니다.

---

## 👨‍💻 개발자 정보

- **개발자**: [YoungJin Kim]
- **이메일**: [yjym33@naver.com]
- **GitHub**: [https://github.com/yjym33](https://github.com/yjym33)
- **블로그**: [https://yjym33.tistory.com/](https://yjym33.tistory.com/)

---

## 🙏 감사의 말

이 프로젝트는 다음 기술과 라이브러리의 도움으로 개발되었습니다:

- **OpenAI**: GPT-4 API 제공
- **NestJS**: 강력한 백엔드 프레임워크
- **Next.js**: 최고의 React 프레임워크
- **TypeORM**: 훌륭한 ORM
- **그 외 모든 오픈소스 기여자분들**

---

## 📞 문의

프로젝트에 대한 문의사항이 있으시면 언제든지 연락주세요!

- **Email**: [yjym33@naver.com]
- **Issue Tracker**: [GitHub Issues](https://github.com/yjym33/ai-chatbot/issues)
- **Discord**: [커뮤니티 링크]

---

**Last Updated**: 2025년 11월 17일  
**Version**: 0.1.0
